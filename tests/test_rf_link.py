import serial
import time
import struct
import threading

def calc_crc8(data):
    CRC8_TABLE = [
        
    ]
    crc = 0
    for b in data:
        crc = CRC8_TABLE[crc ^ b]
    return crc

def build_esp3_frame(packet_type, data, opt_data = b''):
    header = struct.pack(">HBB", len(data), len(opt_data), packet_type)
    crc8_hdr = calc_crc8(header)
    body = data + opt_data
    crc8_data = calc_crc8(body)
    return b'\x55' + header + bytes([crc8_hdr]) + body + bytes([crc8_data])

TCM_PORT = "/dev/serial/by-id/usb-Espressif_USB_JTAG_serial_debug_unit_58:E6:C5:E6:77:28-if00"
EMU_PORT = "/dev/serial/by-id/usb-Espressif_USB_JTAG_serial_debug_unit_98:A3:16:8E:9E:5C-if00"

def listen_and_print(ser, name, duration, expected_signature):
    end_time = time.time() + duration
    success = False
    while time.time() < end_time:
        if ser.in_waiting > 0:
            resp = ser.read(ser.in_waiting)
            hex_str = resp.hex()
            print(f"[{name}] RX: {hex_str}")
            if expected_signature in hex_str:
                success = True
        time.sleep(0.05)
    return success

def main():
    try:
        tcm = serial.Serial(TCM_PORT, 57600, timeout=1)
        emu = serial.Serial(EMU_PORT, 57600, timeout=1)
    except Exception as e:
        print(f"Failed to open ports: {e}")
        return

    # Clear buffers
    tcm.read(tcm.in_waiting)
    emu.read(emu.in_waiting)

    print("\n=== TEST 1: Real TCM515 -> Emulator ===")
    # RPS F6 Data=00 SenderID=FFBA5D80 Status=30
    # Destination=FFFFFFFF (Broadcast)
    # The TCM will reply with RET_OK (0x00) locally, and transmit the packet via RF.
    tcm_tx_data = bytes.fromhex("f600ffba5d8030")
    tcm_tx_opt  = bytes.fromhex("03ffffffff0000")
    tcm_tx_frame = build_esp3_frame(0x01, tcm_tx_data, tcm_tx_opt)
    
    print(f"[Host] Sending to Real TCM515: {tcm_tx_frame.hex()}")
    tcm.write(tcm_tx_frame)
    
    # We expect EMU to receive a Type 1 packet containing 'ffba5d80'
    ok = listen_and_print(emu, "Emulator", 1.5, "ffba5d80")
    if ok:
        print(">> SUCCESS: Emulator received RF packet from Real TCM515!")
    else:
        print(">> FAILED: Emulator did not receive the RF packet.")
        
    print("\n=== TEST 2: Emulator -> Real TCM515 ===")
    # RPS F6 Data=55 SenderID=FFEEDDCC Status=30
    emu_tx_data = bytes.fromhex("f655ffeeddcc30")
    emu_tx_opt  = bytes.fromhex("03ffffffff0000")
    emu_tx_frame = build_esp3_frame(0x01, emu_tx_data, emu_tx_opt)
    
    print(f"[Host] Sending to Emulator: {emu_tx_frame.hex()}")
    emu.write(emu_tx_frame)
    
    # We expect TCM to receive a Type 1 packet containing 'ffeeddcc'
    ok = listen_and_print(tcm, "Real TCM515", 1.5, "ffeeddcc")
    if ok:
        print(">> SUCCESS: Real TCM515 received RF packet from Emulator!")
    else:
        print(">> FAILED: Real TCM515 did not receive the RF packet.")
        
    tcm.close()
    emu.close()

if __name__ == "__main__":
    main()
